using Gtk 4.0;

template ProvidersList : Widget {
  layout-manager: BinLayout {};

  ListBox list {
    selection-mode: none;

    row-activated => _on_provider_activated();

    styles [
        "boxed-list"
    ]
  }
}